// Import all your components here...
import Counter from '../components/Counter';

/**
 * Object format: {
 *  exact: true,
 *  path: '/',
 *  component: Counter,
 * }
 */
const routes = [
  {
    exact: true,
    path: '/',
    component: Counter,
  },
];

export default routes;
